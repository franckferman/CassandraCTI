{#- Briefing body (Markdown — Discord & Teams). The LLM already produced plain
    prose with inline URLs; the transport shows the briefing title as the card
    heading, so this renders just the body. -#}
{{ summary }}
